<Window x:Class="Plana.Desktop.LauncherEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="{DynamicResource AddLauncherHeading}"
        Width="580" Height="520" MinWidth="520" MinHeight="460"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="{DynamicResource ApplicationBackgroundBrush}">
    <Grid Margin="28,24,28,22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="{DynamicResource AddLauncherHeading}" FontSize="22" FontWeight="SemiBold" />
            <TextBlock Text="{DynamicResource LauncherEditorHelp}" Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       TextWrapping="Wrap" Margin="0,5,0,0" />
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,20,0,18">
            <StackPanel>
                <TextBlock Text="{DynamicResource LauncherNameLabel}" FontWeight="SemiBold" />
                <TextBox x:Name="LauncherNameTextBox" Margin="0,6,0,16" />

                <TextBlock Text="{DynamicResource LauncherFolderLabel}" FontWeight="SemiBold" />
                <TextBox x:Name="LauncherFolderTextBox" Margin="0,6,0,16" />

                <TextBlock Text="{DynamicResource LauncherExecutableLabel}" FontWeight="SemiBold" />
                <TextBox x:Name="LauncherExecutableTextBox" Text="wt.exe" Margin="0,6,0,16" />

                <TextBlock Text="{DynamicResource LauncherArgumentsLabel}" FontWeight="SemiBold" TextWrapping="Wrap" />
                <TextBox x:Name="LauncherArgumentsTextBox" Text="-d&#x0a;{folder}" AcceptsReturn="True"
                         Height="104" VerticalScrollBarVisibility="Auto" Margin="0,6,0,4" />
                <TextBlock Text="{DynamicResource LauncherArgumentsHelp}" Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           TextWrapping="Wrap" />
                <TextBlock x:Name="EditorErrorText" Foreground="#B3261E" TextWrapping="Wrap" Margin="0,12,0,0" />
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{DynamicResource CancelButton}" IsCancel="True" Margin="0,0,8,0" />
            <ui:Button Content="{DynamicResource AddLauncherButton}" IsDefault="True" Appearance="Primary" Click="OnAddClick" />
        </StackPanel>
    </Grid>
</Window>
