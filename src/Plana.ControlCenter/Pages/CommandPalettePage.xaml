<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Plana_ControlCenter.Pages.CommandPalettePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Plana_ControlCenter.Pages">
    <Grid Padding="32,24,32,32" RowSpacing="20">
        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
        <StackPanel Spacing="6">
            <TextBlock x:Name="PageTitle" Style="{StaticResource PageTitleStyle}" Text="Quick launch" />
            <TextBlock x:Name="PageDescription" Foreground="{StaticResource PlanaSubtleBrush}" Text="Search Actions, tools, settings, and Chat from one place." />
        </StackPanel>
        <AutoSuggestBox x:Name="SearchBox" Grid.Row="1" QueryIcon="Find" PlaceholderText="Type a command or alias" TextChanged="SearchBox_TextChanged" QuerySubmitted="SearchBox_QuerySubmitted" />
        <Grid Grid.Row="2" RowSpacing="10">
            <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
            <InfoBar x:Name="CommandStatus" IsOpen="False" IsClosable="True" />
            <ListView x:Name="CommandList" Grid.Row="1" ItemsSource="{x:Bind VisibleCommands}" IsItemClickEnabled="True" ItemClick="CommandList_ItemClick" SelectionMode="Single">
                <ListView.Resources>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="MinHeight" Value="64" />
                    </Style>
                </ListView.Resources>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:CommandRow">
                        <Grid Padding="8,8" ColumnSpacing="14">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="40" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /><ColumnDefinition Width="28" /></Grid.ColumnDefinitions>
                            <Border Width="36" Height="36" CornerRadius="8" Background="{ThemeResource ControlFillColorSecondaryBrush}">
                                <FontIcon HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" Glyph="{x:Bind Glyph}" />
                            </Border>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                <TextBlock FontWeight="SemiBold" Text="{x:Bind Title}" TextTrimming="CharacterEllipsis" />
                                <TextBlock FontSize="12" Foreground="{StaticResource PlanaSubtleBrush}" Text="{x:Bind Subtitle}" TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                            <TextBlock Grid.Column="2" VerticalAlignment="Center" Foreground="{StaticResource PlanaSubtleBrush}" Text="{x:Bind Source}" />
                            <FontIcon Grid.Column="3" VerticalAlignment="Center" FontSize="12" Foreground="{StaticResource PlanaSubtleBrush}" Glyph="&#xE76C;" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
                <ListView.Footer>
                    <StackPanel x:Name="EmptyState" Margin="0,56,0,0" HorizontalAlignment="Center" Spacing="8" Visibility="Collapsed">
                        <FontIcon FontSize="28" Glyph="&#xE721;" />
                        <TextBlock x:Name="EmptyTitle" HorizontalAlignment="Center" FontWeight="SemiBold" Text="No matching commands" />
                        <TextBlock x:Name="EmptyDescription" Foreground="{StaticResource PlanaSubtleBrush}" Text="Try a name, alias, source, or description." />
                    </StackPanel>
                </ListView.Footer>
            </ListView>
        </Grid>
    </Grid>
</Page>
